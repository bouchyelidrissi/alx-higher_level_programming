#!/usr/bin/python3
""" check if obj is instance of the specified class"""


def is_kind_of_class(obj, a_class):
    """ check if obj is instance of the specified class"""
    return isinstance(obj, a_class)
